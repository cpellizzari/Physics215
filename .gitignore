# Never commit these
js/config.local.js
*.env
.env*
config.json

# Skill config — each instructor copies config.json.template locally
.claude/skills/preflight-analyze/config.json

# Textbook PDFs — too large for git (~968 MB); download from Teams instead
# See textbook-pdfs/README.md for instructions
textbook-pdfs/**/*.pdf
textbook-pdfs/**/*.PDF

# macOS
.DS_Store

# Supabase service key files (kept in ~/.claude/skills/ only)
*.service_key
